
services:
  proxy:
    image: ubuntu/squid
    volumes:
      - ./squid.conf:/etc/squid/squid.conf
      - ./squid.dev.conf:/etc/squid/squid.dev.conf
    env_file:
      - ./.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - devnet
      - internet
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - HTTP_PROXY=http://proxy:3128
      - HTTPS_PROXY=http://proxy:3128
    networks:
      - devnet
    depends_on:
      - proxy
    user: ubuntu
    working_dir: /home/ubuntu
    volumes:
      - ../:/home/ubuntu/workspace
      - ./.npmrc/:/home/ubuntu/.npmrc
      - ./gradle.properties:/home/ubuntu/.gradle/gradle.properties
    command: sleep infinity

  web:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - devnet
      - ingress_net
    ports:
      - "3000:3000"
      - "3001:3001"
  liferay:
    image: liferay/portal:7.2.1-ga2
    ports:
      - "8080:8080"
    volumes:
      - ./liferay-modules:/opt/liferay/deploy
networks:
  devnet:
    driver: bridge
    internal: true
  internet:
    driver: bridge
  ingress_net:
    driver: bridge
